# This project is a calculator, one specificially designed for programmers
I'll add more to this read me later, but the main idea is to first be able to implement regular calculator features
i.e
  -parethseses
  -operators such as addition subtraction etc.
  -exponents
The type of calculator is similar to the one type in google where a user types in a mathimatical expression.

If the input is valid, a numircal output is displayed. 

The displayed output should also be able to once again have calculations be done on it.

For the main audience, i'd like to cater to programers, so I'd like to also add a tab for programmer specific calculation, i.e bitwise function, diffrent base opertation, etc.

A potieintial "stretch goal" would be visual element such as data structure visualizes.

For now, I want to be able to parse through a string and calculate results!

Already spent some time on paper trying to figuring out the method.

Day 2 update: I would also like to add a temp function storage

Day 3: more clear project goal, 
1. Interactive calulator ui that can be typed, and push bottons to enter values
2. dynmaic design
3. regular calcular tab (with regular math operations)
4. programmer tab (++x, &, potieintially multiple base support)
5. Create your own operators(given the way I coded this, this shouldnt be that hard to design)
But now my calculator can actually do basic operations, which is great! Next time want it have all
the diffrent type (I thought of 3, function based [sin(x), sum(i=x, n = y, f = z)], as well as unary operators
[x!, x%]), also want to get grouping aspects such as () or [], which I already have an idea for.

But overall good progress! nice!

Not much of a ReadME, but hey it's my first one!

